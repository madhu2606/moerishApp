import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moerish.app',
  appName: 'Moerish',
  webDir: 'dist\\MoerishApp',
  bundledWebRuntime: false
};

export default config;
